import React from 'react';
import ReactDOM from 'react-dom';

ReactDOM.render(
  <React.StrictMode>
    <div>prepare the react project to start</div>
  </React.StrictMode>,
  document.getElementById('root')
);